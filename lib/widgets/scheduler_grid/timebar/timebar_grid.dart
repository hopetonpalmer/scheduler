import 'package:flutter/material.dart';

class TimebarGrid extends StatelessWidget {
  final Axis direction;
  const TimebarGrid({super.key, required this.direction});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
