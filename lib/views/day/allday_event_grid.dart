import 'package:flutter/material.dart';

class AlldayEventGrid extends StatelessWidget {
  const AlldayEventGrid({required Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
